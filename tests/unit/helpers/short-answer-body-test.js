
import { shortAnswerBody } from 'message-board-ember/helpers/short-answer-body';
import { module, test } from 'qunit';

module('Unit | Helper | short answer body');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = shortAnswerBody([42]);
  assert.ok(result);
});

