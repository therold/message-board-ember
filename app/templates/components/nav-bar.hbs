<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">

    <ul class="nav navbar-nav nav-pills">
      <li>{{#link-to 'index' class="navbar-brand"}}Epic Overflow{{/link-to}}</li>
    </ul>

    {{#if userService.currentUser}}
      <ul class="nav navbar-nav nav-pills pull-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" >Welcome, {{userService.currentUser.name}} <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li>{{#link-to 'new-question'}}New Question{{/link-to}}</li>
            <li {{action 'logout'}}><a href="#">Log out</a></li>
          </ul>
        </li>
      </ul>
      {{else}}
      <ul class="nav navbar-nav nav-pills pull-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" >Account <span class="caret"></span></a>
          <ul class="dropdown-menu dropdown-menu-right">
            <li>{{#link-to 'login'}}Log in{{/link-to}}</li>
            <li>{{#link-to 'new-user'}}Add account{{/link-to}}</li>
          </ul>
        </li>
      </ul>
    {{/if}}

  </div>
</nav>
