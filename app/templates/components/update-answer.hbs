<form {{action 'update' answer on='submit'}}>

  <div class="form-group">
    <label for="body">Answer</label>
    {{textarea value=body id="body" class="form-control" rows=8 required="required"}}
  </div>

    <div class="form-group">
      <label for="author">Your name</label>
      {{input value=author id="author" class="form-control" required="required"}}
    </div>

  <button class="btn btn-warning">Update Answer</button>
  <button class="btn btn-default" {{action 'cancel'}}>Cancel</button>
  <button class="btn btn-danger pull-right" {{action 'delete' answer}}>Delete Answer</button>
</form>
